# Private key of the deployer (from Hardhat accounts or custom wallet for testnet)
WALLET_PRIVATE_KEY=0xyourLocalOrTestnetPrivateKey

# For local Hardhat node (optional if using localhost in config)
RPC_URL=http://127.0.0.1:8545

# Sepolia RPC (via Infura, Alchemy, or custom)
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_PROJECT_ID

# Etherscan API key (for verifying contracts on Sepolia)
ETHERSCAN_API_KEY=your_etherscan_api_key
